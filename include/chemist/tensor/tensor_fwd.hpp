#pragma once

namespace chemist::tensor {

template<typename FieldType>
class Allocator;

template<typename FieldType>
class TensorWrapper;

template<typename FieldType>
class Shape;

} // namespace chemist::tensor
